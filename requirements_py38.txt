# ========================================
# 城市出行数据分析系统 - Python 3.8.12 依赖清单
# Python 3.8.12 (2021-08-30)
# 安装方式: pip install -r requirements_py38.txt
#
# ⚠️ 注意: pandas>=2.0 和 numpy>=1.24 已不再支持 Python 3.8
#         因此降级至最后兼容 Python 3.8 的版本
# ========================================

# ========================================
# 核心数据处理
# ========================================
pandas==1.5.3          # 2.0+ 需要 Python>=3.9, 最后兼容版
numpy==1.23.5          # 1.24+ 需要 Python>=3.9, 最后兼容版

# ========================================
# 可视化
# ========================================
matplotlib==3.7.5      # 3.7.x 最后版本, 兼容 Python 3.8
seaborn==0.13.2        # 0.13.x 最后版本
folium==0.14.0         # 0.14.x 新增功能最多, 兼容 Python 3.8

# ========================================
# 机器学习 (聚类)
# ========================================
scikit-learn==1.3.2    # 1.3.x 最后版本, 兼容 Python 3.8

# ========================================
# Web框架
# ========================================
flask==2.2.5           # 2.2.x 最后版本
werkzeug==2.2.3        # flask 2.2.x 依赖上限

# ========================================
# 数据库 (CentOS VM MySQL连接)
# ========================================
pymysql==1.1.1         # 最新稳定版

# ========================================
# 工具库 (自动安装的依赖也锁定)
# ========================================
scipy==1.10.1          # scikit-learn 依赖, 最后兼容 Python 3.8
joblib==1.3.2          # scikit-learn 依赖
Pillow==10.0.1         # matplotlib 依赖, 10.x 最后支持 3.8
jinja2==3.1.4          # flask 依赖

# ========================================
# 地理空间处理 (可选)
# 取消注释以安装:
# ========================================
# geopandas==0.9.0
# shapely==1.8.5
# fiona==1.8.22

# ========================================
# 交互式可视化 (可选)
# ========================================
# plotly==5.18.0

# ========================================
# PySpark (VM环境, 可选)
# ========================================
# pyspark==3.5.3
